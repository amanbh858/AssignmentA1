version: '3.8'

services:
  app:
    build: .
    container_name: my_flask_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app/app.py
      - FLASK_RUN_HOST=0.0.0.0
    command: python app/app.py
